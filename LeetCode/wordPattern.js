/**
 * @param {string} pattern
 * @param {string} s
 * @return {boolean}
 */
const wordPattern = function (pattern, s) {
    const alphabet = 'abcdefghijklmnopqrstuvwxyz'
    const result = {}


    

    alphabet.indexOf(pattern)
};